import {StyleSheet} from 'react-native'

import genericStyles from '../../../styles/generic'

const styles = StyleSheet.create({
  body: {
    padding: 10
  },
  errorMessage: genericStyles.error
})

export default styles
